int main () {


    // create a bunch of chatrooms
    // have lists of chatroom, i.e, only dog, only cat, cat and dog etc.
    // when user is created, they pick list of which they want to join, i.e dog, cat, cat and dog
    // this is because a list of chatrooms is required as a parameter for creating a user
    // therefore create user can be deffered to the, like, signup class or something, new class will have to be made
    // combine create user/factory with command?
    // nothing for now
    return 0;
}